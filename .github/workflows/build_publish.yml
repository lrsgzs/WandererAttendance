name: Build & Publish Multi-Platform

on:
  push:
    branches:
      - master
  workflow_dispatch:
    inputs:
      release_tag:
        description: "发布标签（例如 1.0.0.0）"
        required: true
        type: string

permissions:
  contents: read

jobs:
  build_desktop:
    name: Build_Desktop_${{ matrix.os }}_${{ matrix.arch }}_${{ matrix.build_type }}
    runs-on: ${{ matrix.os == 'linux' && 'ubuntu-24.04' || (matrix.os == 'macos' && 'macos-15' || 'windows-2022') }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows, linux, macos]
        arch: [x64, x86, arm64]
        build_type: [full, selfContained]
        exclude:
          - os: linux
            arch: x86
          - os: macos
            arch: x86
          - os: linux
            build_type: full
          - os: macos
            build_type: full

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref }}

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Restore
        run: dotnet restore WandererAttendance.Desktop/WandererAttendance.Desktop.csproj

      - name: Publish Desktop
        shell: pwsh
        env:
          RID: ${{ matrix.os == 'windows' && format('win-{0}', matrix.arch) || (matrix.os == 'linux' && format('linux-{0}', matrix.arch) || format('osx-{0}', matrix.arch)) }}
          SELF_CONTAINED: ${{ matrix.build_type == 'selfContained' }}
          MODE: ${{ matrix.build_type == 'selfContained' && 'sc' || 'fdd' }}
        run: |
          $outDir = "dist/desktop/$env:RID-$env:MODE"
          dotnet publish WandererAttendance.Desktop/WandererAttendance.Desktop.csproj `
            -c Release `
            -r $env:RID `
            --self-contained $env:SELF_CONTAINED `
            -p:PublishSingleFile=true `
            -p:PublishTrimmed=false `
            -p:UseAppHost=true `
            --no-restore `
            -o $outDir

      - name: Pack Desktop
        shell: pwsh
        env:
          RID: ${{ matrix.os == 'windows' && format('win-{0}', matrix.arch) || (matrix.os == 'linux' && format('linux-{0}', matrix.arch) || format('osx-{0}', matrix.arch)) }}
          MODE: ${{ matrix.build_type == 'selfContained' && 'sc' || 'fdd' }}
        run: |
          New-Item -ItemType Directory -Path dist -Force | Out-Null
          $source = "dist/desktop/$env:RID-$env:MODE/*"
          $target = "dist/WandererAttendance_desktop_$env:RID`_$env:MODE.zip"
          Compress-Archive -Path $source -DestinationPath $target -Force

      - name: Upload Desktop Artifact
        uses: actions/upload-artifact@v4
        with:
          name: desktop_${{ matrix.os }}_${{ matrix.arch }}_${{ matrix.build_type }}
          path: dist/WandererAttendance_desktop_*.zip

  build_android:
    name: Build_Android_apk
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref }}

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Install Android workload
        run: dotnet workload install android

      - name: Restore
        run: dotnet restore WandererAttendance.Android/WandererAttendance.Android.csproj

      - name: Publish Android
        run: |
          dotnet publish WandererAttendance.Android/WandererAttendance.Android.csproj \
            -c Release \
            -f net9.0-android35.0 \
            -p:AndroidPackageFormat=apk \
            --no-restore \
            -o dist/android

      - name: Collect APK
        shell: pwsh
        run: |
          $apk = Get-ChildItem -Path dist/android -Recurse -Filter *.apk | Select-Object -First 1
          if (-not $apk) { throw "No APK found in dist/android" }
          Copy-Item $apk.FullName dist/WandererAttendance_android.apk -Force

      - name: Upload Android Artifact
        uses: actions/upload-artifact@v4
        with:
          name: android_apk
          path: dist/WandererAttendance_android.apk

  build_browser:
    name: Build_Browser
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref }}

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Install wasm-tools workload
        run: dotnet workload install wasm-tools

      - name: Restore
        run: dotnet restore WandererAttendance.Browser/WandererAttendance.Browser.csproj

      - name: Publish Browser
        run: |
          dotnet publish WandererAttendance.Browser/WandererAttendance.Browser.csproj \
            -c Release \
            --no-restore \
            -o dist/browser_publish

      - name: Prepare Browser Artifacts and CNAME
        shell: pwsh
        run: |
          $wwwroot = "dist/browser_publish/wwwroot"
          if (-not (Test-Path $wwwroot)) { throw "Browser wwwroot not found: $wwwroot" }
          "wanderer.lrsgzs.top" | Out-File -FilePath "$wwwroot/CNAME" -Encoding ascii -NoNewline
          Compress-Archive -Path "$wwwroot/*" -DestinationPath "dist/WandererAttendance_browser.zip" -Force

      - name: Upload Browser Artifact
        uses: actions/upload-artifact@v4
        with:
          name: browser_package
          path: dist/WandererAttendance_browser.zip

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/browser_publish/wwwroot

  deploy_browser_pages:
    name: Deploy_Browser_Pages
    runs-on: ubuntu-latest
    needs: build_browser
    if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag }}
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  publish:
    name: Publish_Release
    runs-on: windows-2022
    needs: [build_desktop, build_android, build_browser]
    if: ${{ always() && success('build_desktop') && success('build_android') && success('build_browser') && github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag && github.event_name != 'pull_request' }}
    permissions:
      contents: write
    concurrency:
      group: publish-public
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.inputs.release_tag }}

      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./out_artifacts

      - name: Combine Artifacts
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path ./out -Force | Out-Null
          Get-ChildItem ./out_artifacts -Recurse -File -Include *.zip,*.apk | ForEach-Object {
            Copy-Item $_.FullName ./out -Force
          }
          Write-Host "Combined files:"
          Get-ChildItem ./out -File | Select-Object Name, Length

      - name: Generate Release Note
        env:
          tagName: ${{ github.event.inputs.release_tag }}
        run: pwsh -ep bypass ./scripts/gen-release-note.ps1

      - name: Upload APP to release
        uses: ncipollo/release-action@v1
        with:
          artifacts: "./out/*.zip,./out/*.apk"
          draft: true
          bodyFile: ./release-note.md
          token: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.event.inputs.release_tag }}

